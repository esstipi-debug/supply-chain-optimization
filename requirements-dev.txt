# Development dependencies (tests, coverage, linting).
# Runtime deps live in requirements.txt.
-r requirements.txt
pytest>=7.0
pytest-cov>=4.0
ruff>=0.4
# web UI (webapp/) + its test client
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
python-multipart>=0.0.9  # required by POST /api/jobs (Form/File); app import fails without it
